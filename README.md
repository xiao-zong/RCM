# possible journal to submit
1. Annals of Applied Probability
2. Journal of Functional Analysis




# Comments for next meeting:
1. we need a new notation for $M_{i,j}(f)$, as $M_{x_i,x_j}$ being used for marks in edge-marked Poisson process
2. define $\mathrm{d}_{\rm TV}$, $\mathrm{d}_{\rm Wass}$, $\mathrm{d}_{\rm K}$
3. Unify the notation for balls in $\mathbb{R}^d$, i.e., $B^d(0,r)$. In the Power-weighted edge-length application, $B(0,\rho)$ stands for a ball in $\mathbb{R}^d$ with radius $\rho$, and $B^2(0,\rho)$ for $(B(0,\rho))^2$. In fact, the same issue occurs in the Euclidean distance $\|\cdot\|$, in hub-to-hub, we use $\|\cdot\|^d$, instead $\|\cdot\|$.
4. The asymptotic notation $\sim$ need to be defined.
5. In the Power-weighthed edge-length example, the intensity $\gamma$ for the driven Poisson process is being missed.
6. Question to Ercan: in the bottom line of Page 20, last sentence, "We can assume that $\varphi$ is only radially dependent...", is "We can assume" or "We **need** assume..."
7. Should we change "Remark 6.4" to a corollary?
8. 